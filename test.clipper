FUNCTION main()
    LOCAL start = TimeHnsecs(0)
    LOCAL end = 0
    LOCAL elapsed = 0

    DO WHILE elapsed < 5000
        end := TimeHnsecs(0)
        elapsed := end - start
        elapsed := elapsed / 1000

        ? elapsed
    ENDDO
RETURN 0
